{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["eslint", "typescript", "unicorn", "oxc", "vue", "vitest"],
  "env": {
    "browser": true
  },
  // The skill bundles are vendored reference material, not code we ship or maintain.
  "ignorePatterns": [".claude/**"],
  "categories": {
    "correctness": "error"
  }
}
